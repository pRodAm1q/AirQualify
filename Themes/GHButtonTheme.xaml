<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="{x:Type Button}"
           x:Key="GHButton">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Height="100" Width="200">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,2">
                                <GradientStop Color="#8f20f7" Offset="0.0"/>
                                <GradientStop Color="#7309d6" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.Clip>
                            <RectangleGeometry RadiusX="30" RadiusY="35" Rect="0,0,200,100"/>
                        </Border.Clip>
                        <TextBlock FontSize="22" Foreground="White" Text="View on GitHub" HorizontalAlignment="Center" VerticalAlignment="Center">
                              
                        </TextBlock>
                    </Border> 
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>